server:
  port: 8081

spring:
  application:
    name: opinions
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/opinions
    username: root
    password: wsch8001
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  client: 
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
  instance:
    hostname: localhost